# CounterDown
## Backend Setup
In the root directory, run the following command to start the backend:
```bash
npm start

## Frontend Setup
for the froentend go to the client directory and run the 
npm run start
